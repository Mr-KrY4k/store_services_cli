# Store Services CLI

Утилита для управления конфигурациями магазинов приложений (GMS, HMS) в Flutter-проекте.
Позволяет быстро переключаться между Google Play и Huawei AppGallery, а также настраивать гибридные сборки.

## Установка и Запуск

Убедитесь, что у вас установлен Dart SDK.

1. Откройте терминал в корне вашего Flutter-проекта.
2. Запустите CLI, указав путь к этому пакету, или скомпилируйте его.

**Запуск из исходников:**
```bash
dart run path/to/store_services_cli
```

**Если CLI находится внутри проекта:**
```bash
dart run bin/store_services_cli.dart
```

## Использование

После запуска появится интерактивное меню:

```text
🚀 Store Services CLI
---------------------
1. GMS (Google Play Services)
2. HMS (Huawei Mobile Services)
3. Hybrid (GMS + HMS)
4. Clean (Удалить конфигурации)
---------------------
```

### Режимы работы

*   **1. GMS**: Настраивает проект для Google Play.
    *   Добавляет плагины `com.google.gms.google-services`, `firebase_crashlytics`.
    *   Подключает зависимости Firebase и Play Services.
    *   Прописывает разрешения (`AD_ID`, `ACCESS_FINE_LOCATION`) в манифесте.
*   **2. HMS**: Настраивает проект для Huawei AppGallery.
    *   Добавляет репозиторий Huawei в `settings.gradle.kts` и `build.gradle.kts`.
    *   Подключает плагин `agconnect`.
    *   Удаляет "опасные" для Huawei разрешения (`REQUEST_INSTALL_PACKAGES`).
    *   Добавляет git-зависимости (`huawei_push`, `huawei_ads` и др.) в `pubspec.yaml`.
*   **3. Hybrid**: Включает обе конфигурации одновременно.
*   **4. Clean**: Удаляет все внесенные изменения, возвращая проект в "чистое" состояние (без привязок к сторам).

## Важно

Утилита модифицирует следующие файлы:
*   `android/settings.gradle.kts`
*   `android/build.gradle.kts`
*   `android/app/build.gradle.kts`
*   `android/app/src/main/AndroidManifest.xml`
*   `pubspec.yaml`

**Рекомендуется делать коммит или бэкап перед использованием инструмента.**
Секретные файлы (`google-services.json`, `agconnect-services.json`) вы должны добавить в проект самостоятельно.
